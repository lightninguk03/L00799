# 🎨 LETAVERSE 前端设计规范

> 赛博朋克风格设计系统文档
>
> **版本**: V1.0
> **更新日期**: 2025-12-16

---

## 1. 设计理念

### 核心关键词

- **高科技低生活** (High-Tech Low-Life)
- **霓虹黑色电影** (Neon Noir)
- **毛玻璃效果** (Glassmorphism)
- **沉浸式 HUD** (Heads-Up Display)

### 视觉风格

- 纯黑/深蓝背景
- 高饱和度霓虹色点缀（青色 & 紫色）
- 多层级界面深度感
- 动态感：扫描线、呼吸光效、微交互

### 界面层级

```
背景图 → 暗色遮罩 → 毛玻璃面板 → 霓虹内容 → HUD 覆盖层
```

---

## 2. 配色方案

### 主要颜色

| 名称   | Hex 值      | CSS 变量          | 用途               |
| ------ | ----------- | ----------------- | ------------------ |
| 霓虹紫 | `#8a2be2` | `--neon-purple` | 主色调、按钮、边框 |
| 赛博青 | `#00ffff` | `--cyber-cyan`  | 强调色、链接、图标 |
| 玫红   | `#ff0055` | `--accent-pink` | 次级高亮、副标题   |
| 霓虹黄 | `#fcee0a` | `--neon-yellow` | 警告、特殊标记     |

### 背景颜色

| 名称     | Hex 值                     | 用途       |
| -------- | -------------------------- | ---------- |
| 深黑     | `#0a0a10`                | 主背景     |
| 深紫黑   | `#10121b`                | 卡片背景   |
| 半透明黑 | `rgba(16, 18, 27, 0.85)` | 毛玻璃背景 |

### 文字颜色

| 名称     | Hex 值      | 用途             |
| -------- | ----------- | ---------------- |
| 主文字   | `#eeeeee` | 正文内容         |
| 次要文字 | `#888888` | 元数据、辅助说明 |
| 霓虹文字 | `#00ffff` | 高亮文字         |

### CSS 变量定义

```css
:root {
  --neon-purple: #8a2be2;
  --cyber-cyan: #00ffff;
  --accent-pink: #ff0055;
  --neon-yellow: #fcee0a;
  --bg-dark: #0a0a10;
  --bg-card: #10121b;
  --glass-bg: rgba(16, 18, 27, 0.85);
  --glass-border: rgba(138, 43, 226, 0.4);
  --text-main: #eeeeee;
  --text-muted: #888888;
}
```

---

## 3. 字体排印

### 字体家族

| 用途      | 字体     | 备选                  |
| --------- | -------- | --------------------- |
| 标题/展示 | Orbitron | sans-serif            |
| 正文      | Roboto   | Rajdhani, sans-serif  |
| 代码/数据 | Inter    | Space Mono, monospace |

### 字体大小

| 级别  | 大小            | 用途       |
| ----- | --------------- | ---------- |
| h1    | 2.5rem (40px)   | 页面主标题 |
| h2    | 2rem (32px)     | 区块标题   |
| h3    | 1.5rem (24px)   | 卡片标题   |
| body  | 1rem (16px)     | 正文       |
| small | 0.875rem (14px) | 辅助文字   |
| xs    | 0.75rem (12px)  | 元数据     |

### 标题样式

```css
.cyber-title {
  font-family: 'Orbitron', sans-serif;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  text-shadow: 0 0 10px rgba(0, 255, 255, 0.5);
}
```

---

## 4. 毛玻璃效果

### 基础样式

```css
.glass-panel {
  background: rgba(16, 18, 27, 0.85);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border: 1px solid rgba(138, 43, 226, 0.4);
  box-shadow: 0 0 15px rgba(0, 0, 0, 0.5);
  border-radius: 8px;
}
```

### 变体

| 变体 | 透明度 | 模糊度 | 用途       |
| ---- | ------ | ------ | ---------- |
| 轻度 | 0.6    | 8px    | 悬浮提示   |
| 标准 | 0.85   | 10px   | 卡片、面板 |
| 重度 | 0.95   | 20px   | 模态框     |

---

## 5. 布局系统

### 全局背景

```css
.cyber-background {
  background: 
    linear-gradient(rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.8)),
    url('/bg_cyberpunk.jpg');
  background-size: cover;
  background-position: center;
  background-attachment: fixed;
}
```

### HUD 覆盖层

固定在 UI 顶层的装饰层：

- **角落框架**: 屏幕四角的 L 型标记
- **状态文本**: "SYSTEM ONLINE", "SYNC RATE: 99.9%"
- **扫描线**: 水平青色光线从上至下循环扫描

```css
.hud-overlay {
  position: fixed;
  inset: 0;
  z-index: 100;
  pointer-events: none;
}

.hud-corner {
  position: absolute;
  width: 40px;
  height: 40px;
  border: 2px solid rgba(0, 255, 255, 0.3);
}

.hud-corner-tl {
  top: 10px;
  left: 10px;
  border-right: none;
  border-bottom: none;
}
```

---

## 6. UI 组件规范

### 6.1 按钮 (NeonButton)

#### 主要按钮

```css
.neon-button-primary {
  background: linear-gradient(135deg, #8a2be2, #00ffff);
  color: #000;
  font-weight: bold;
  text-transform: uppercase;
  font-family: 'Orbitron', sans-serif;
  border-radius: 50px;
  padding: 12px 24px;
  box-shadow: 0 0 15px rgba(138, 43, 226, 0.4);
  transition: all 0.3s ease;
}

.neon-button-primary:hover {
  transform: scale(1.05);
  box-shadow: 0 0 25px rgba(138, 43, 226, 0.6);
}
```

#### 次要按钮

```css
.neon-button-secondary {
  background: transparent;
  color: #00ffff;
  border: 1px solid #00ffff;
  border-radius: 50px;
  padding: 12px 24px;
}

.neon-button-secondary:hover {
  background: rgba(0, 255, 255, 0.1);
  box-shadow: 0 0 15px rgba(0, 255, 255, 0.3);
}
```

### 6.2 输入框 (CyberInput)

```css
.cyber-input {
  background: rgba(0, 0, 0, 0.7);
  border: 1px solid #8a2be2;
  color: #00ffff;
  padding: 12px 16px;
  border-radius: 4px;
  transition: all 0.3s ease;
}

.cyber-input:focus {
  border-color: #00ffff;
  transform: translateY(-2px);
  box-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
}

.cyber-input::placeholder {
  color: #666;
}
```

### 6.3 卡片 (CyberCard)

```css
.cyber-card {
  background: rgba(16, 18, 27, 0.85);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(138, 43, 226, 0.4);
  border-radius: 8px;
  padding: 20px;
  transition: all 0.3s ease;
}

.cyber-card:hover {
  border-color: #d536ff;
  transform: translateY(-5px);
  box-shadow: 0 10px 30px rgba(138, 43, 226, 0.3);
}
```

### 6.4 模态框

```css
.cyber-modal {
  background: rgba(16, 18, 27, 0.95);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(138, 43, 226, 0.6);
  border-radius: 12px;
  clip-path: polygon(
    20px 0, 100% 0, 100% calc(100% - 20px),
    calc(100% - 20px) 100%, 0 100%, 0 20px
  );
}
```

---

## 7. 动画效果

### 7.1 扫描线

```css
@keyframes scanline {
  0% { top: -100%; }
  100% { top: 100%; }
}

.cyber-scanline::after {
  content: '';
  position: absolute;
  width: 100%;
  height: 2px;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(0, 255, 255, 0.5),
    transparent
  );
  animation: scanline 4s linear infinite;
}
```

### 7.2 霓虹闪烁

```css
@keyframes neon-flicker {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.8; }
  52% { opacity: 1; }
  54% { opacity: 0.9; }
}

.neon-flicker {
  animation: neon-flicker 3s infinite;
}
```

### 7.3 呼吸发光

```css
@keyframes neon-pulse {
  0%, 100% {
    box-shadow: 0 0 5px rgba(0, 255, 255, 0.5);
  }
  50% {
    box-shadow: 0 0 20px rgba(0, 255, 255, 0.8);
  }
}

.neon-pulse {
  animation: neon-pulse 2s ease-in-out infinite;
}
```

### 7.4 故障效果

```css
@keyframes glitch {
  0%, 100% { transform: translate(0); }
  20% { transform: translate(-2px, 2px); }
  40% { transform: translate(-2px, -2px); }
  60% { transform: translate(2px, 2px); }
  80% { transform: translate(2px, -2px); }
}

.glitch-hover:hover {
  animation: glitch 0.3s ease-in-out;
}
```

### 7.5 心跳效果

```css
@keyframes heartbeat {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.1); }
}

.heartbeat {
  animation: heartbeat 1.5s ease-in-out infinite;
}
```

---

## 8. 响应式设计

### 断点

| 名称 | 宽度    | 说明     |
| ---- | ------- | -------- |
| xs   | < 375px | 小屏手机 |
| sm   | 640px   | 手机     |
| md   | 768px   | 平板     |
| lg   | 1024px  | 桌面     |
| xl   | 1280px  | 大屏桌面 |

### 移动端适配

```css
/* HUD 层移动端 */
@media (max-width: 768px) {
  .hud-corner {
    width: 30px;
    height: 30px;
  }
  
  .hud-status-text {
    font-size: 10px;
  }
}

/* 模态框移动端 */
@media (max-width: 768px) {
  .cyber-modal {
    width: 100%;
    height: 100%;
    border-radius: 0;
    clip-path: none;
  }
}
```

---

## 9. 图标规范

### 图标库

使用 Lucide React 图标库

### 图标大小

| 场景     | 大小 |
| -------- | ---- |
| 导航图标 | 24px |
| 按钮图标 | 20px |
| 列表图标 | 16px |
| 徽章图标 | 12px |

### 图标颜色

- 默认: `#888888`
- 悬停: `#00ffff`
- 激活: `#8a2be2`

---

## 10. 间距系统

### 基础单位

基础单位: 4px

### 间距值

| 名称 | 值   | 用途     |
| ---- | ---- | -------- |
| xs   | 4px  | 紧凑间距 |
| sm   | 8px  | 小间距   |
| md   | 16px | 标准间距 |
| lg   | 24px | 大间距   |
| xl   | 32px | 区块间距 |
| 2xl  | 48px | 页面间距 |

---

## 11. 阴影系统

### 阴影层级

```css
/* 轻度阴影 */
.shadow-cyber-sm {
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
}

/* 标准阴影 */
.shadow-cyber {
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.4);
}

/* 重度阴影 */
.shadow-cyber-lg {
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.5);
}

/* 霓虹阴影 */
.shadow-neon-purple {
  box-shadow: 0 0 15px rgba(138, 43, 226, 0.4);
}

.shadow-neon-cyan {
  box-shadow: 0 0 15px rgba(0, 255, 255, 0.4);
}
```

---

## 12. 资源清单

### 必需图片

| 文件               | 用途       | 建议尺寸              |
| ------------------ | ---------- | --------------------- |
| logo.jpg           | 网站 Logo  | 任意，CSS 控制        |
| favicon.jpg        | 浏览器图标 | 32x32 或 64x64        |
| bg_cyberpunk.jpg   | 全局背景   | 1920x1080+            |
| bg_hero.jpg        | Hero 背景  | 1920x1080+            |
| kanban_girl.png    | AI 看板娘  | 透明 PNG，高度 600px+ |
| default_avatar.jpg | 默认头像   | 200x200               |

### 字体文件

- Orbitron (Google Fonts)
- Roboto (Google Fonts)
- Rajdhani (Google Fonts)

---

**最后更新**: 2025-12-16
