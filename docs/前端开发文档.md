# ⚡ LETAVERSE 前端开发文档

> 赛博朋克风格 ACG 社交元宇宙平台 - 前端技术文档
>
> **版本**: V2.9
> **更新日期**: 2025-12-16

---

## 1. 项目概述

LETAVERSE（莱塔宇宙）是一个具有赛博朋克（Cyberpunk）风格的 ACG 社交平台前端项目。项目注重视觉效果（霓虹、玻璃拟态、动态交互）和响应式体验。

### 1.1 核心特性

- **极致赛博朋克视觉**: 霓虹紫/青配色、毛玻璃效果、HUD 覆盖层、动态扫描线
- **完整社交功能**: 发布/编辑/删除/转发动态、点赞、评论、关注
- **AI 助手 Mu**: 智能对话、悬浮聊天窗口、全屏对话页面
- **用户系统**: 注册、登录、邮箱验证、密码重置、个人资料
- **通知系统**: 点赞、评论、关注、转发通知
- **国际化**: 中英双语切换
- **响应式设计**: 完美适配桌面端和移动端

### 1.2 技术栈

| 类别        | 技术                | 版本    |
| ----------- | ------------------- | ------- |
| 核心框架    | React + TypeScript  | 19.x    |
| 构建工具    | Vite                | 7.x     |
| 样式方案    | Tailwind CSS        | 4.x     |
| 状态管理    | TanStack Query      | 5.x     |
| 路由        | React Router DOM    | 7.x     |
| 动画        | Framer Motion       | 12.x    |
| 国际化      | i18next             | 25.x    |
| HTTP 客户端 | Axios               | 1.x     |
| 图标        | Lucide React        | 0.560.x |
| 测试        | Vitest + fast-check | 4.x     |

---

## 2. 项目结构

```
frontend/
├── src/
│   ├── assets/              # 静态资源
│   │   ├── ai_avatar.png          # AI 头像
│   │   ├── bg_cyberpunk.jpg       # 赛博朋克背景
│   │   ├── bg_hero.jpg            # 首页 Hero 背景
│   │   ├── bg_main.jpg            # 主背景
│   │   ├── default_avatar.jpg     # 默认用户头像
│   │   ├── kanban_girl.png        # 看板娘图片
│   │   ├── logo.jpg               # Logo
│   │   ├── mu_ai_kanban.png       # MuAI 立绘
│   │   └── react.svg              # React 图标
│   │
│   ├── components/          # 可复用组件
│   │   ├── ai/              # AI 相关组件
│   │   │   └── ChatWidget.tsx     # Mu AI 悬浮聊天窗口
│   │   ├── auth/            # 认证组件
│   │   │   └── RequireAuth.tsx    # 路由保护组件
│   │   ├── landing/         # 落地页组件
│   │   │   └── HeroSection.tsx    # 首页 Hero 区域
│   │   ├── layout/          # 布局组件
│   │   │   ├── HolographicLayout.tsx  # 全局布局
│   │   │   ├── NavBar.tsx             # 导航栏
│   │   │   ├── AnimatedRoutes.tsx     # 路由动画
│   │   │   └── ScrollToTop.tsx        # 滚动到顶部
│   │   └── ui/              # 通用 UI 组件
│   │       ├── NeonButton.tsx         # 霓虹按钮
│   │       ├── CyberCard.tsx          # 赛博卡片
│   │       ├── CyberInput.tsx         # 赛博输入框
│   │       ├── GlitchText.tsx         # 故障文字
│   │       ├── HUDOverlay.tsx         # HUD 覆盖层
│   │       ├── PostCard.tsx           # 动态卡片
│   │       ├── MasonryGrid.tsx        # 瀑布流布局
│   │       ├── ContentSlider.tsx      # 内容轮播
│   │       ├── CreatePostModal.tsx    # 发布动态弹窗
│   │       ├── EditPostModal.tsx      # 编辑动态弹窗
│   │       ├── EditProfileModal.tsx   # 编辑资料弹窗
│   │       ├── RepostModal.tsx        # 转发弹窗
│   │       ├── ReportModal.tsx        # 举报弹窗
│   │       └── AnnouncementBanner.tsx # 系统公告
│   │
│   ├── config/              # 配置文件
│   │   └── site.config.ts         # 站点默认配置
│   │
│   ├── contexts/            # React Context
│   │   └── SiteConfigContext.tsx  # 站点配置上下文
│   │
│   ├── hooks/               # 自定义 Hooks
│   │   ├── useAuth.ts             # 认证状态
│   │   └── useScrollPosition.ts   # 滚动位置
│   │
│   ├── lib/                 # 工具函数
│   │   └── utils.ts               # 通用工具
│   │
│   ├── locales/             # 国际化翻译
│   │   ├── zh.json                # 中文
│   │   └── en.json                # 英文
│   │
│   ├── pages/               # 页面组件
│   │   ├── Home.tsx               # 首页
│   │   ├── Community.tsx          # 社区
│   │   ├── MuAI.tsx               # AI 助手页
│   │   ├── AIChatPage.tsx         # AI 聊天页（嵌入模式）
│   │   ├── Profile.tsx            # 个人中心
│   │   ├── UserProfile.tsx        # 用户主页
│   │   ├── Settings.tsx           # 设置
│   │   ├── Notifications.tsx      # 通知
│   │   ├── MyPosts.tsx            # 我的动态
│   │   ├── PostDetail.tsx         # 动态详情
│   │   ├── Gallery.tsx            # 图库
│   │   ├── Search.tsx             # 搜索
│   │   ├── FollowList.tsx         # 关注/粉丝列表
│   │   ├── Login.tsx              # 登录
│   │   ├── Register.tsx           # 注册
│   │   ├── VerifyEmail.tsx        # 邮箱验证
│   │   ├── ForgotPassword.tsx     # 忘记密码
│   │   ├── ResetPassword.tsx      # 重置密码
│   │   ├── Banned.tsx             # 封禁提示
│   │   └── Activity.tsx           # 活动页
│   │
│   ├── __tests__/           # 测试文件
│   │   ├── setup.ts               # 测试配置
│   │   └── properties/            # 属性测试
│   │
│   ├── api.ts               # API 接口定义
│   ├── i18n.ts              # 国际化配置
│   ├── index.css            # 全局样式
│   ├── App.tsx              # 应用入口
│   └── main.tsx             # 主入口
│
├── public/                  # 公共资源
│   ├── favicon.jpg
│   └── vite.svg
│
├── vite.config.ts           # Vite 配置
├── tailwind.config.js       # Tailwind 配置
├── tsconfig.json            # TypeScript 配置
├── vitest.config.ts         # Vitest 配置
└── package.json             # 依赖管理
```

---

## 3. 核心功能模块

### 3.1 布局系统

#### HolographicLayout（全局布局）

唯一的全站布局容器，负责：

- 统一管理赛博朋克背景图
- 集成 HUD 覆盖层（四角装饰、扫描线）
- 包含导航栏（根据认证状态自动显示/隐藏）
- 动态设置 Favicon 和页面标题

#### NavBar（导航栏）

- **桌面端**: 顶部固定导航栏
- **移动端**: 底部 Tab 栏
- 显示 Logo、导航链接、用户头像、通知徽章

#### AnimatedRoutes（路由动画）

使用 Framer Motion 实现页面过渡动画：

- 淡入淡出效果
- 页面飞入/飞出
- 列表交错进入

### 3.2 认证系统

#### 双 Token 机制

- `access_token`: 用于 API 请求
- `refresh_token`: 用于刷新 access_token
- 自动刷新：401 错误时自动使用 refresh_token 刷新

#### 路由保护

```tsx
// RequireAuth 组件包裹需要登录的路由
<Route path="/profile" element={
  <RequireAuth>
    <Profile />
  </RequireAuth>
} />
```

#### useAuth Hook

```tsx
const { isLoggedIn, user, requireAuth } = useAuth();

// 需要登录才能执行的操作
if (!requireAuth(undefined, '请先登录')) return;
```

### 3.3 站点配置（SiteConfigContext）

从后端 `/system/config` 获取动态配置：

```tsx
const { config, refreshConfig } = useSiteConfig();
const { i18n } = useTranslation();
const isZh = i18n.language.startsWith('zh');

// 获取品牌信息
const siteName = isZh ? config.siteNameCn : config.siteName;

// 获取视觉资源
import { getMediaUrl } from '../lib/utils';
const logo = getMediaUrl(config.logo);
```

**自动刷新机制**：

- 每 5 分钟自动刷新
- 页面切换回来时自动刷新
- 提供 `refreshConfig()` 手动刷新

### 3.4 动态系统

| 功能 | 组件            | 说明                 |
| ---- | --------------- | -------------------- |
| 发布 | CreatePostModal | 支持文本、图片、视频 |
| 编辑 | EditPostModal   | 仅可编辑文本内容     |
| 删除 | PostCard        | 内置删除确认         |
| 转发 | RepostModal     | 可添加转发评论       |
| 互动 | PostCard        | 点赞、收藏、评论     |

### 3.5 通知系统

- **类型**: 点赞、评论、关注、转发
- **功能**: 单条已读、全部已读、点击跳转
- **显示**: 导航栏未读数量徽章

### 3.6 AI 助手

- **ChatWidget**: 悬浮聊天窗口，支持拖拽
- **MuAI 页面**: 全屏对话界面，赛博朋克终端风格

---

## 4. 后端配置字段

### 4.1 完整配置清单

| 字段                     | 类型     | 用途          | 使用位置                      |
| ------------------------ | -------- | ------------- | ----------------------------- |
| `site_name`            | string   | 网站英文名    | NavBar, 浏览器标签            |
| `site_name_cn`         | string   | 网站中文名    | 首页                          |
| `slogan`               | string   | 英文标语      | HeroSection                   |
| `slogan_cn`            | string   | 中文标语      | HeroSection                   |
| `logo`                 | string   | 网站 Logo     | NavBar                        |
| `favicon`              | string   | 浏览器图标    | 浏览器标签                    |
| `background`           | string   | 全局背景图    | HolographicLayout             |
| `hero_background`      | string   | Hero 单张背景 | HeroSection (回退)            |
| `hero_banners`         | string[] | Hero 轮播图   | HeroSection (优先)            |
| `ai_kanban`            | string   | AI 看板娘     | HeroSection, ChatWidget, MuAI |
| `default_avatar`       | string   | 默认头像      | 无头像用户                    |
| `ai.name`              | string   | AI 英文名     | ChatWidget, MuAI              |
| `ai.name_cn`           | string   | AI 中文名     | ChatWidget, MuAI              |
| `ai.title`             | string   | AI 英文头衔   | ChatWidget, MuAI              |
| `ai.title_cn`          | string   | AI 中文头衔   | ChatWidget, MuAI              |
| `ai.greeting`          | string   | AI 英文欢迎语 | ChatWidget, MuAI              |
| `ai.greeting_cn`       | string   | AI 中文欢迎语 | ChatWidget, MuAI              |
| `social_links`         | array    | 社交链接      | 首页底部                      |
| `world_database`       | object   | 世界观数据库  | 首页                          |
| `world_database_media` | string[] | 世界观媒体    | 首页左侧                      |
| `announcement`         | object   | 系统公告      | 首页 Hero 下方                |
| `thumbnail`            | string   | 视频封面图    | PostCard, Profile             |

### 4.2 系统公告配置

```json
{
  "announcement": {
    "enabled": true,
    "type": "info",
    "title": "系统公告",
    "titleEn": "Announcement",
    "content": "欢迎来到闪电社区！",
    "contentEn": "Welcome to Lightning Community!",
    "link": "/community",
    "linkText": "了解更多",
    "linkTextEn": "Learn More"
  }
}
```

### 4.3 世界观数据库配置

```json
{
  "world_database": {
    "title": "World Database",
    "title_cn": "世界观数据库",
    "subtitle": "Database",
    "subtitle_cn": "数据库",
    "archive_title": "📂 Archive: 2025-RE",
    "archive_title_cn": "📂 档案记录: 2025-RE",
    "paragraphs": [
      {
        "title": "Reality Collapse",
        "title_cn": "现实崩塌",
        "content": "In the future timeline...",
        "content_cn": "在未来的时间线中..."
      }
    ],
    "cards": [
      {
        "title": "Gen-Z Creators",
        "title_cn": "Z世代创作者",
        "description": "Native community of the metaverse.",
        "description_cn": "这里是元宇宙的原住民社区。"
      }
    ]
  }
}
```

---

## 5. API 对接说明

### 5.1 分页格式

所有列表接口返回统一格式：

```json
{
  "total": 100,
  "page": 1,
  "page_size": 20,
  "items": [...]
}
```

### 5.2 媒体 URL 处理

```typescript
import { getMediaUrl, getAllMediaUrls, getAvatarUrl } from '../lib/utils';

// 获取第一张图片
const imageUrl = getMediaUrl(post.media_urls);

// 获取所有图片
const allImages = getAllMediaUrls(post.media_urls);

// 获取头像 URL
const avatarUrl = getAvatarUrl(user.avatar);
```

### 5.3 错误处理

```typescript
// 统一错误格式
{
  "error_code": "invalid_credentials",
  "message": "用户名或密码错误"
}

// 使用工具函数获取本地化错误消息
import { getApiErrorMessage } from '../lib/utils';
const message = getApiErrorMessage(error, t);
```

### 5.4 限流保护

前端会处理后端返回的 429 限流错误，显示"操作太频繁，请稍后再试"提示。

---

## 6. 赛博朋克动态效果

### 6.1 CSS 动画类名

| 类名                  | 效果         | 适用场景         |
| --------------------- | ------------ | ---------------- |
| `neon-flicker`      | 霓虹闪烁     | 标题、状态文字   |
| `neon-pulse`        | 青色呼吸发光 | 按钮、图标       |
| `neon-pulse-purple` | 紫色呼吸发光 | 按钮、装饰       |
| `glitch-hover`      | 悬停故障抖动 | 按钮、可点击元素 |
| `data-flow`         | 数据流动画   | 背景、分割线     |
| `border-glow`       | 边框呼吸     | 卡片、输入框     |
| `hologram`          | 全息抖动     | AI 元素、头像    |
| `card-scan`         | 卡片扫描线   | 卡片组件         |
| `particle`          | 浮动粒子     | 背景装饰         |
| `rgb-split-hover`   | RGB分离悬停  | 标题、按钮       |
| `circuit-bg`        | 电路板纹理   | 背景             |
| `cute-bounce`       | 可爱弹跳     | 加载动画         |
| `heartbeat`         | 心跳效果     | 状态指示         |
| `gradient-border`   | 渐变边框     | 分割线           |
| `float-card`        | 悬浮效果     | 卡片悬停         |
| `energy-core-btn`   | 能量核心     | 主要按钮         |

### 6.2 使用示例

```tsx
// 霓虹闪烁标题
<h1 className="text-cyber-cyan neon-flicker">SYSTEM ONLINE</h1>

// 呼吸发光按钮
<button className="neon-pulse">点击</button>

// 心跳状态指示
<span className="w-2 h-2 bg-cyber-cyan rounded-full heartbeat" />

// 卡片扫描线 + 悬浮效果
<div className="card-scan float-card">卡片内容</div>

// 加载动画
<div className="flex space-x-1">
  <div className="w-2 h-2 bg-cyber-cyan rounded-full cute-bounce" style={{ animationDelay: '0s' }} />
  <div className="w-2 h-2 bg-neon-purple rounded-full cute-bounce" style={{ animationDelay: '0.1s' }} />
  <div className="w-2 h-2 bg-cyber-cyan rounded-full cute-bounce" style={{ animationDelay: '0.2s' }} />
</div>
```

---

## 7. 响应式适配

### 7.1 断点设计

| 断点 | 宽度   | 说明     |
| ---- | ------ | -------- |
| sm   | 640px  | 小屏手机 |
| md   | 768px  | 平板     |
| lg   | 1024px | 桌面     |
| xl   | 1280px | 大屏桌面 |

### 7.2 移动端优化

- **iOS 安全区域**: 底部导航栏适配刘海屏
- **触摸目标**: 最小 44x44px
- **输入框防缩放**: 字体大小固定 16px
- **滚动优化**: `-webkit-overflow-scrolling: touch`

---

## 8. 开发命令

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview

# 运行测试
npm run test

# 监听模式测试
npm run test:watch

# 代码检查
npm run lint
```

---

## 9. 更新日志

### V2.9 (2025-12-16)

#### 首页社区动态移动端适配

- PreviewCard 组件完全重构移动端响应式布局
- 卡片宽度响应式调整：140px → 180px → 280px → 320px
- 标题区域优化，"查看更多"按钮在手机端简化为"更多"

#### 视频缩略图支持

- PostResponse 类型新增 `thumbnail` 字段
- 优先使用后端返回的 thumbnail 作为视频封面
- 回退到 `video#t=0.1` 方式获取视频第一帧

### V2.8 (2025-12-16)

#### 页面滚动位置修复

- 新增 ScrollToTop 组件，解决页面滚动位置问题

#### 动态 Favicon 和页面标题

- HolographicLayout 支持从后端配置动态设置

#### 首页 Slogan 支持

- HeroSection 支持从后端配置动态显示品牌标语

#### 系统公告支持

- AnnouncementBanner 组件支持从后端配置获取系统公告

### V2.7 (2025-12-16)

#### 世界观数据库动态配置

- 世界观数据库内容完全从后台配置加载
- 支持中英双语切换

#### 世界观数据库媒体区域

- 支持单张图片、单个视频、多图/视频轮播
- 无配置时显示默认 GLOBAL NET 动画

#### Bug 修复

- 视频展示优化：添加 poster 属性
- 分享功能修复：多重降级方案
- 发帖按钮与模态框修复

### V2.6 (2025-12-14-15)

#### 赛博朋克动态效果大更新

- 新增大量 CSS 动画效果
- 社区页面能量核心风格发帖按钮
- MuAI 页面全息玻璃终端风格重构
- 首页背景粒子效果

#### 移动端适配优化

- iOS 安全区域支持
- 触摸目标优化
- 输入框防缩放

#### 文件上传类型限制

- 只允许上传图片和视频

#### 用户主页导航功能

- PostCard 用户头像/用户名可点击跳转

### V2.5 (2025-12-14)

#### 动态配置系统

- 前端完全支持后台管理配置
- 品牌信息、AI 助手、视觉资源动态配置

### V2.4 (2025-12-13)

#### 管理功能

- 系统公告功能
- 举报功能
- 封禁检测与提示页面

### V2.3 (2025-12-13)

- 适配后端 `media_urls` 数组格式
- 添加 429 限流错误处理
- 实现设置页面
- 添加编辑/转发动态功能

### V2.2 (2025-12-13)

- 邮箱验证页面
- 忘记密码页面
- 删除动态功能
- 头像上传功能

### V2.1 (2025-12-12)

- 双 Token 认证机制
- 路由保护
- 用户关注系统
- 通知系统

### V2.0 (2025-12-12)

- 赛博朋克 UI 重构
- Mu AI 聊天功能
- 动态发布系统
- 国际化支持

---

**最后更新**: 2025-12-16
